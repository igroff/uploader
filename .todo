make a simple deploy script to tar up the contents of the app and push it out to a remote location
configure the proxy temp path stuff in nginx to allow downloads via uploader (or map to an nginx path)
